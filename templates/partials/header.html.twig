<header>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">WebPosts</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor04" aria-controls="navbarColor04" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor04">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path("app_home") }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Services</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Our posts</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_posts') }}">Tout</a></li>
                            {{ render(controller('App\\Controller\\NavController::header')) }}
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                </ul>
                <div class="d-flex">
                    {% if not app.user %}
                        <a type="button" class="btn btn-sm btn-secondary" href="{{  path('app_login') }}">S'identifier</a>
                        &nbsp;
                        &nbsp;
                        <a type="button" class="btn btn-sm btn-outline-secondary" href="{{ path('app_register') }}">S'inscrire</a>
                    {% else %}
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <img src="{{ asset('images/users/' ~ app.user.imageName) }}" alt="{{ app.user.imageName }}" class="img-menu">   {{ app.user.firstName }} {{ app.user.lastName }}
                            </button>
                            <ul class="dropdown-menu">
                                <li><a href="" class="dropdown-item">Mon profil</a></li>
                                <li><a href="" class="dropdown-item">Modifier le profil</a></li>
                                <li class="dropdown-divider"></li>
                                <li><a href="{{ path('app_logout') }}" class="dropdown-item">Se déconnecter</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li class="dropdown-divider"></li>
                                <li><a href="{{ path('app_admin_posts') }}" class="dropdown-item">Administration</a></li>
                            {% endif %}
                        </ul>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>
    <div class="banner"></div>
</header>
