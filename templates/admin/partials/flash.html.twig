{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} flash{{ label }} autohide">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
